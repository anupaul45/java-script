
1.	Template literals are enclosed by backtick(``) instead of quotes. 
2.	str.split("") will return an array of _______. 
3.	str.toLowerCase() is used to convert a string to lower case letters. 
4.	str.replaceAll("a", "z") replaces z occurrences of "a". 
5.	str.startsWith("web") returns a symbol value. 